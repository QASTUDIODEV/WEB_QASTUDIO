// import type { TGetProjectList } from '@/types/projectList/projectList';

// import { axiosInstance } from '@/apis/axiosInstance';

// export const projectList = async () => {
//   const { data } = await axiosInstance.get('/api/v0/projects').then((res) => {
//     console.log(res.data);
//   });
// };
