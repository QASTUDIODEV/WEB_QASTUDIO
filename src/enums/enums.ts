// 프로젝트 호환 디바이스
export enum DEVICE {
  PC = 'pc',
  MOBILE = 'mobile',
}
// 프로젝트 스택
export enum STACK {
  NEXT = 'next',
  REACT = 'react',
  JS = 'js',
}

//소셜 로그인
export enum SOCIAL {
  KAKAO = 'KAKAO',
  GITHUB = 'GITHUB',
  LOCAL = 'LOCAL',
  GOOGLE = 'GOOGLE',
}
