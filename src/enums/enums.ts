export enum DEVICE {
  PC = 'PC',
  MOBILE = 'MOBILE',
}
// 프로젝트 스택
export enum STACK {
  NEXT = 'NEXT',
  REACT = 'REACT',
  JS = 'JS',
}
// 액션 상태
export enum ACTION_STATE {
  SUCCESS = 'SUCCESS',
  FAIL = 'FAIL',
  UNVERIFIED = 'UNVERIFIED',
}
// 액션 상태
export enum ACTION_TYPE {
  NAVIGATE = 'navigate',
  CLICK = 'click',
  SEND_KEYS = 'send_keys',
}

//소셜 로그인
export enum SOCIAL {
  KAKAO = 'KAKAO',
  GITHUB = 'GITHUB',
  LOCAL = 'LOCAL',
  GOOGLE = 'GOOGLE',
}

export enum TEST_STATE {
  ALL = 'ALL',
  FAIL = 'FAIL',
  SUCCESS = 'SUCCESS',
}
