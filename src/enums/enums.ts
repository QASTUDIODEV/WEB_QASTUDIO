// 프로젝트 호환 디바이스
export enum DEVICE {
  PC = 'pc',
  MOBILE = 'mobile',
}
// 프로젝트 스택
export enum STACK {
  NEXT = 'next',
  REACT = 'react',
  JS = 'js',
}
// 액션 상태
export enum ACTION_STATE {
  SUCCESS = 'success',
  ERROR = 'error',
  UNVERIFIED = 'null',
}

//소셜 로그인
export enum SOCIAL {
  KAKAO = 'KAKAO',
  GITHUB = 'GITHUB',
  LOCAL = 'LOCAL',
  GOOGLE = 'GOOGLE',
}

export enum TEST_STATE {
  FAIL = 'FAIL',
  SUCCESS = 'SUCCESS',
}
