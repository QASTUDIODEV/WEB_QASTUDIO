// import { useQuery } from '@tanstack/react-query';

// import type { TGetProjectList } from '@/types/projectList/projectList';

// import { projectList } from '@/apis/sidebar/sidebar';

// export function useGetProjectList() {
//   const {
//     data: projectListData,
//     isLoading,
//     error,
//   } = useQuery<TGetProjectList, Error>({
//     queryKey: ['projectList'],
//     queryFn: projectList,
//   });

//   return { projectListData, isLoading, error };
// }
