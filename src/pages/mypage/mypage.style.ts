import styled from 'styled-components';

const Container = styled.div`
  display: flex;
`;
const ProfileWrapper = styled.div`
  display: flex;
`;

const UserInfo = styled.div`
  display: flex;
`;
const Account = styled.div`
  display: flex;
`;
export { Account, Container, ProfileWrapper, UserInfo };
